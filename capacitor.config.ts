import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'portal-to-work-client',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
